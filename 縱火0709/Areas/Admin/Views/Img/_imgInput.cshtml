@model PagedList.IPagedList<縱火0709.Models.ImgClass>

@using (Html.BeginForm("index", "Img", FormMethod.Post, new { @enctype = "multipart/form-data" }))
{
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true, "", new { @class = "text-danger" })
    <div class="container" style="margin-top: 20px;">
        <div class="row">
            <div class="col-sm-4 p-l-50">
                <label for="file" class="custom-file">
                    @Html.TextBox("file", "", new { type = "file", @class = "custom-file-input" })
                    <span class="custom-file-control"></span>
                </label>
                <br>
                @Html.ValidationMessage("imgName", new { @class = "text-danger" })
            </div>
            <div class=" col-sm-6 pr-0">
                @Html.TextBox("addImgTxt", "", new { @class = "form-control" })
            </div>
            <div class=" col-sm-2 mb-4">
                <input type="submit" value="Add" class="btn btn-danger addbtn" />
            </div>
        </div>
    </div>
    }